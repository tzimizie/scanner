yfinance>=0.2.40
pandas>=2.1
numpy>=1.26
requests>=2.31
lxml>=5.1
# yfinance pulls these dynamically — pinning them here so the frozen build
# always has them.
multitasking
frozendict
platformdirs
beautifulsoup4
html5lib
# Windows toast notifications for the watch loop. Install fails gracefully
# elsewhere; the runtime check uses a try/except.
win10toast; sys_platform == "win32"
